'use strict'; // List method names
//   iface - <Object>, to be introspected
// Returns: <string[]>, method names

var methods = function methods(iface) {
  var names = [];

  for (var name in iface) {
    if (typeof iface[name] === 'function') {
      names.push(name);
    }
  }

  return names;
}; // List property names
//   iface - <Object>, to be introspected
// Returns: <string[]>, property names


var properties = function properties(iface) {
  var names = [];

  for (var name in iface) {
    if (typeof iface[name] !== 'function') {
      names.push(name);
    }
  }

  return names;
};

module.exports = {
  methods: methods,
  properties: properties
};